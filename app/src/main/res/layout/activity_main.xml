<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
xmlns:tools="http://schemas.android.com/tools"
android:layout_width="match_parent"
android:layout_height="match_parent"
android:paddingBottom="@dimen/activity_vertical_margin"
android:paddingLeft="@dimen/activity_horizontal_margin"
android:paddingRight="@dimen/activity_horizontal_margin"
android:paddingTop="@dimen/activity_vertical_margin"
tools:context=".MainActivity" >

    <EditText
        android:id="@+id/singleCity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Alexandria,USA" />

    <Button
        android:id="@+id/singleCityButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Single City"
        android:onClick="startSingleCityActivity"
        android:layout_below="@id/singleCity"
        android:layout_alignLeft="@id/singleCity"
        />

    <Button
        android:id="@+id/startMultiCityActivityButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Multi City"
        android:layout_below="@id/singleCityButton"
        android:layout_alignLeft="@id/singleCity"
        android:onClick="startMultiCityActivity"
        />

    <TextView
        android:id="@+id/currentLocationTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Current Location "
        android:layout_below="@id/startMultiCityActivityButton"
        />

    <TextView
        android:id="@+id/currentLocationTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Current Location "
        android:layout_toRightOf="@id/currentLocationTitle"
        />


</RelativeLayout>
